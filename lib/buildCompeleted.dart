import 'package:flutter/material.dart';

class buildCompleted extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFDEDDBC3),
      appBar: AppBar(
        title: const Text('SignUp Succeded'),
      ),
    );
  }
}
